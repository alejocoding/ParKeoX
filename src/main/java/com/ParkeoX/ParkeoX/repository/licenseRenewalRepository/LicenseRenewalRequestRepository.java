package com.ParkeoX.ParkeoX.repository.licenseRenewalRepository;

import com.ParkeoX.ParkeoX.models.LicenseRenewalRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LicenseRenewalRequestRepository extends JpaRepository<LicenseRenewalRequest, Long> {
}
